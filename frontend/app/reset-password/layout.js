export const metadata = {
  title: "Reset your password — FarmPulse",
  description:
    "Recover your FarmPulse account: request a one-time code by email, verify it, and choose a new password.",
  openGraph: {
    title: "Reset your password — FarmPulse",
    description: "Secure OTP-based password recovery for FarmPulse.",
  },
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
